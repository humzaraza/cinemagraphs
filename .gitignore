# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
# testing
/coverage
# next.js
/.next/
/out/
# production
/build
# misc
.DS_Store
*.pem
# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
# env files (can opt-in for committing if needed)
.env
.env.local
.env.production
!.env.example
# vercel
.vercel
# typescript
*.tsbuildinfo
next-env.d.ts
/src/generated/prisma
node_modules_old
.env*.local
# uploaded avatars
/public/avatars/
.neon
# hybrid-sentiment local verification output (scripts/test-hybrid.ts)
hybrid-test-output.txt
# bulk hybrid regeneration checkpoint (scripts/bulk-regen-hybrid.ts)
.hybrid-regen-checkpoint.json
.hybrid-regen-checkpoint.json.tmp
.claude/
/*.png

# Carousel scratch outputs (never commit)
spike-resvg.mjs
spike-output/
graph-renderer-output/
slide-composer-output/
test-horizontal.webm
test-vertical.webm

# Local-only diagnostic scripts (already-tracked files in this dir remain tracked)
scripts/diagnostic/
